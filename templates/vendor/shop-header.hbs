<header class="sheet-header">
  <img class="document-image" src="{{actor.img}}" alt="{{actor.name}}" />
  <div class="header-details">
    <h1 class="actor-name">{{actor.name}}</h1>
  </div>
</header>
